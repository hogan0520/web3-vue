{
  "private": true,
  "name": "example-cra",
  "scripts": {
    "start": "react-scripts start",
    "pin": "sh -c '{ for X in {core,empty}; do lerna add @web3-vue/$X --exact --no-bootstrap --scope=example-cra; done }'"
  },
  "dependencies": {
    "@types/react-dom": "^18.0.0",
    "@web3-vue/core": "0.0.1",
    "@web3-vue/empty": "0.0.1",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
